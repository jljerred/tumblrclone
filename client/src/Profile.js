//contains a banner image, pfp, display name, handle, URL, and bio section
// button will display "follow" if current user does not follow them, if user does follow them button will read "Following"
//displays a feed of reblogged and original posts 

const Profile = ({})=>{
    return (
        <h1>profile</h1>
    )
}

export default Profile;