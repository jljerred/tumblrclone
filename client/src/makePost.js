//make new post here
//clicking link in upper right hand corner of header directs here 
//is visible with post type options (text, photo, quote, link, chat, audio, video) on Dashboard

const makePost = ({})=>{
    return (
        <h1>makePost</h1>
    )
}

export default makePost;